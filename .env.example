# Leave unset to use the Vite dev proxy (see vite.config.ts), which forwards
# /api/* to http://localhost:8000. Set this only if the backend runs elsewhere
# (e.g. a deployed API URL for production builds).
# VITE_API_BASE_URL=http://localhost:8000
