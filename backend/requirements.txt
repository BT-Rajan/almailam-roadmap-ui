fastapi==0.140.0
uvicorn[standard]==0.51.0
SQLAlchemy==2.0.51
PyMySQL==1.2.0
cryptography==44.0.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.0.1
PyJWT==2.10.1
bcrypt==4.2.1
email-validator==2.3.0
python-multipart==0.0.20
httpx==0.28.1
APScheduler==3.11.3
# Text extraction for the knowledgebase Q&A feature (app/services/
# knowledge_extract.py) -- pulls plain text out of uploaded PDF/DOCX
# files (including Arabic text layers) so it can be grounded into the
# LLM prompt. Both PDF extraction here and Anthropic/DeepSeek's Arabic
# handling only cover text-layer PDFs, not scanned/image-only pages --
# there is no OCR step in this pipeline.
pypdf==5.1.0
python-docx==1.1.2
# Merges live project data into an admin-uploaded .docx template to
# produce the actual downloadable Quotation/Contract document (see
# app/services/document_template_service.py) -- Jinja2-style {{ field }}
# placeholders and {%tr for ... %} row loops inside the .docx XML,
# rendered back out as a real .docx. Pure-Python, built on python-docx
# above (already a dependency) -- no extra system libraries needed.
docxtpl==0.19.0
# zoneinfo (used for the Kuwait-time report-filing cutoff, see
# status_report_service.py) needs an IANA tz database on the host --
# present by default on most Linux distros but not guaranteed in
# minimal/slim container images. tzdata bundles it directly so the
# lookup never silently falls back to server-local time.
tzdata==2025.2
# HTML -> PDF rendering for a filled government-form template (see
# app/services/pdf_render.py) -- turns a merged {{token}} template into
# a real, printable PDF saved as a project Document, not just an
# on-screen preview. Unlike tzdata above, this is NOT a pure-Python
# wheel: it needs Pango, Cairo, and GDK-Pixbuf shared libraries already
# installed on the host (e.g. Debian/Ubuntu: libpango-1.0-0,
# libpangocairo-1.0-0, libcairo2, libgdk-pixbuf-2.0-0). install.sh
# deliberately never runs apt-get itself (see its own header comment),
# so these must already be present on the server -- if `pip install -r
# requirements.txt` succeeds but importing weasyprint fails, that's the
# missing piece, not a Python problem.
weasyprint==69.0
# Turns a merged Quotation/Contract .docx into semantic HTML (see
# document_template_service._docx_to_pdf), which is then handed to
# WeasyPrint above to produce the PDF "Print"/"Email" actually use --
# so those show the admin's real uploaded-template content instead of a
# separate hardcoded preview. Pure-Python, no system dependency (unlike
# weasyprint above): a LibreOffice-headless docx->PDF conversion was
# tried first and does not run reliably in this app's container, hence
# this HTML round-trip instead of a direct docx->PDF tool.
mammoth==1.12.1
# Server-side sanitizer for the rich-text content saved from Quotation/
# Contract preview editors (scope summary, clauses, notes, terms) --
# strips anything outside the small formatting allowlist (bold/italic/
# underline, font-size spans, images) before it's stored, since the
# client-side DOMPurify pass can't be trusted as the only line of
# defense against a script tag reaching the database.
nh3==0.3.7
